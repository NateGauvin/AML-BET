name: process_upload

on:
  push:
    branches: main

jobs:
  test-process:
    runs-on: ubuntu-latest
    steps:
      - name: set up R
        uses: r-lib/actions/setup-r@v2

      - name: install packages
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::dplyr
            any::ggplot2
            any::tidyverse
            any::MASS
            any::mongolite
            any::argparse

      - name: add repo
        uses: actions/checkout@v3

      - name: process TCGA no upload
        run: Rscript functions/commandLineParser.r --db no --datasets "TCGA"
